
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. In the Configuration panel, open "Captioning Backend" and paste your Gemini API key. It is stored in a browser cookie (no .env needed).
